"use strict";

var React = require('react/addons');

var CartCount = React.createClass({
  render: function() {
    return <span className="ns-CartCount">4</span>;
  }
});

module.exports = CartCount;
